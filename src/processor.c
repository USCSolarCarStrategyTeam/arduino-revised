#include "processor.h"

int process(struct Processor* processor) {
    // Access reader's data

    // Check for critical failure

    // Convert data into forms needed for Writer
        // This responsibility /might/ be assigned to writer
        // Will depend on how we communicate output types
}
